# Install Bundles

Available install bundles:

- Windows: `INSTALL/windows/README.md`

The Windows bundle now also includes an Inno Setup project and a generated `Setup.exe` under `dist/windows-installer/`.
