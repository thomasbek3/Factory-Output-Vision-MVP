import { PlaceholderPage } from "@/components/chrome/placeholder-page";

export default function StationsPage() {
  return (
    <PlaceholderPage
      title="Stations"
      body="Station analytics is coming in the next update — your per-station counts live on the Live tab for now."
      actionLabel="Go to Live"
      actionHref="/"
    />
  );
}
